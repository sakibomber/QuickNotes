# dependencies
node_modules/

# build output (CI builds this from source on every push)
dist/
dist-*/

# test scratch
.test-build/

# packaged uploads
*.zip

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# local env
.env
.env.local
*.local

# build contract + design references — kept locally, not published
documents/
